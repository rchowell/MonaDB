[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "monadb"
dynamic = ["version"]
description = "MonaDB — an embedded, dict-like SQL engine with a Python API."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "Apache-2.0" }
authors = [{ name = "R. Conner Howell", email = "rchowell19@gmail.com" }]
keywords = ["database", "sql", "embedded", "document-store"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Rust",
    "Topic :: Database",
]
[project.urls]
Homepage = "https://github.com/rchowell/MonaDB"
Repository = "https://github.com/rchowell/MonaDB"
Documentation = "https://github.com/rchowell/MonaDB#readme"

[tool.maturin]
features = ["python"]
module-name = "monadb._monadb"
python-source = "."

[tool.pytest.ini_options]
testpaths = ["monadb/tests"]

[dependency-groups]
dev = ["pytest>=8", "maturin>=1.7,<2"]
