{
  "name": "monadb-site",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i input.css -o static/style.css --minify",
    "dev": "tailwindcss -i input.css -o static/style.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}
