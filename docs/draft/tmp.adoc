= TMP

Each session can create tables/views/etc. in a `tmp` namespace, which is appended to the start of the path. This enables a user

[source,sql]
----
use tmp;

-- table at /tmp/my_tmp_table
create table my_tmp_table ({ ... });
----
